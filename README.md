# Koshya

A Vite + React single-page corporate gifting site.

## Development

```powershell
npm install
npm run dev
```

## Production build

```powershell
npm run build
npm run preview
```

Deploy the generated `dist` directory. The custom-domain configuration is copied from `public/CNAME` into the build automatically.
